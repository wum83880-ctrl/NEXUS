import { NextRequest, NextResponse } from "next/server";
import { getRoom, updateRoom, deleteRoom, type GroupMember } from "@/lib/nexus/group-chat";
export async function GET(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const room = await getRoom(id); if (!room) return NextResponse.json({ error: "不存在" }, { status: 404 }); return NextResponse.json({ room }); }
export async function PATCH(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const body = await req.json().catch(() => ({})); const patch: any = {}; if (typeof body.name === "string") patch.name = body.name; if (Array.isArray(body.members)) patch.members = body.members as GroupMember[]; if (typeof body.task === "string") patch.task = body.task; if (typeof body.status === "string") patch.status = body.status; if (Object.keys(patch).length === 0) return NextResponse.json({ error: "没有可更新的字段" }, { status: 400 }); const room = await getRoom(id); if (!room) return NextResponse.json({ error: "不存在" }, { status: 404 }); await updateRoom(id, patch); return NextResponse.json({ ok: true }); }
export async function DELETE(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; await deleteRoom(id); return NextResponse.json({ ok: true }); }
